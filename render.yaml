services:
  - type: web
    name: metro-voc-dashboard
    runtime: python
    pythonVersion: 3.11
    buildCommand: "pip install -r requirements.txt"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
