services:
  - type: web
    name: metro-voc-dashboard
    runtime: python
    buildCommand: "echo 'Dashboard ready'"
    startCommand: "python -m http.server 8000"
    envVars:
      - key: PORT
        value: "8000"
